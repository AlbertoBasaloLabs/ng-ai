# Environment Setup

```bash
# install and select node version
fnm i v20.19.2
fnm use v20.19.2
node -v
# install angular
npm i @angular/cli@17.3.17 -g
ng v
# create new angular project with ssr, routing and css
ng new astro-bookings --directory=./ -g --ssr --routing --style=css -d
````

